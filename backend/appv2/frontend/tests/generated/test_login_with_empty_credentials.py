# Generated by the aivar orchestration agent
# Flow ID: f4
# Flow name: Login with empty credentials
# Regenerate rather than edit by hand

import os
from playwright.sync_api import Page, expect

def test_login_with_empty_credentials(page: Page):
    page.goto('https://penguin-olive-sigma.vercel.app/')
    # action: Sign In
    page.get_by_role('link', name='Sign In').first.click()
    # assertion: student sign in
    expect(page.get_by_role('link', name='Sign In').first).to_be_visible()
